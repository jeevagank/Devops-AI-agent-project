---
- name: Restart Jenkins
  systemd:
    name: jenkins
    state: restarted
    daemon_reload: true

- name: Reload JCasC
  uri:
    url: "http://localhost:{{ jenkins_port }}/reload-configuration-as-code/?casc-reload-token=true"
    method: POST
    status_code: 200
