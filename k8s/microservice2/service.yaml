apiVersion: v1
kind: Service
metadata:
  name: microservice2
  namespace: microservice2
  labels:
    app: microservice2
    project: telstra
spec:
  selector:
    app: microservice2
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
