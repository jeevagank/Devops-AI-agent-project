apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: microservice2
  namespace: microservice2
  labels:
    app: microservice2
    project: telstra
spec:
  podSelector:
    matchLabels:
      app: microservice2
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ingress-nginx
        - podSelector:
            matchLabels:
              app: microservice2
      ports:
        - protocol: TCP
          port: 8080
  egress:
    - to:
        - namespaceSelector: {}
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    - to:
        - podSelector:
            matchLabels:
              app: microservice2
      ports:
        - protocol: TCP
          port: 8080
