apiVersion: v1
kind: Service
metadata:
  name: microservice4
  namespace: microservice4
  labels:
    app: microservice4
    project: telstra
spec:
  selector:
    app: microservice4
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
