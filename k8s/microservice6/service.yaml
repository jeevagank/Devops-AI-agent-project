apiVersion: v1
kind: Service
metadata:
  name: microservice6
  namespace: microservice6
  labels:
    app: microservice6
    project: telstra
spec:
  selector:
    app: microservice6
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
