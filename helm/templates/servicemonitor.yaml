apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ .Values.image.repository }}
spec:
  selector:
    matchLabels:
      app: {{ .Values.image.repository }}
  endpoints:
  - port: http
