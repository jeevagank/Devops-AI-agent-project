# Alerting rules for memory usage
groups:
- name: memory_usage.rules
  rules:
  - alert: HighMemoryUsage
    expr: avg(rate(container_memory_usage_bytes[1m])) > 1000000000
    for: 5m
    labels:
      severity: warning
    annotations:
      summary: High memory usage for container {{ $labels.container }}
